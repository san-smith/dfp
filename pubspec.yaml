name: fp
description: A library for typed functional programming in Dart, inspired by Rust.
version: 0.0.1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:

dev_dependencies:
  lints: ^2.0.0
  test: ^1.17.12
